spring:
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: validate
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: schema_create.sql
  datasource:
    platform: postgres
    url: jdbc:postgresql://192.168.99.100:5432/forecast_dev
    username: sample_user
    password: testpw
    driverClassName: org.postgresql.Driver